按**“可以直接照着执行 + 后面可以直接写进论文 Experimental Protocol”**的标准，把剩余实验完整设计出来。

我建议你从现在开始把这些实验视为一轮paper-final validation freeze：实验方案、随机种子、输入变体、统计方法、判定标准先全部写死，再开始跑。这样以后面对审稿人时，你可以明确说明：这些实验不是看到结果后临时改规则，而是在运行前预先固定的 validation protocol。

你当前主任务本身是一个 conditional repair setting：事件已经定位、相关 evidence window 已提供、正确修复保证存在于有限 candidate set 中。 原始 v5 又存在 oracle 全部为 CAND_002 的构造规律，不过 full-online per-event permutation 已经打破了这个 literal-ID shortcut。 因此下面涉及 candidate selection 的新实验，原则上都优先以 candidate-id-permute variant 为父 benchmark。

一、先建立统一的 Paper-Final Validation Protocol

在跑 Exp8–Exp15 之前，先建立：

G:\LearnAI\ontology-evolution\output\paper-final-validation\
    validation-freeze\

建议里面至少有：

validation-protocol.md
validation-freeze.json
validation-freeze.sha256

validation-protocol.md 写清楚：

1. Parent benchmark
2. Method version
3. Frozen prompts
4. Frozen thresholds
5. Random seeds
6. Number of runs
7. Backend
8. Statistical methods
9. Exclusion policy
10. Timeout/error policy
11. No-rerun policy
12. No-post-result-tuning policy
统一原则

所有新增实验都遵守：

不修改 M13
不修改 M14
不修改 M15
不修改 M16
不修改 ranking weights
不修改 ranking thresholds
不修改 candidate construction after seeing predictions
不修改 closure rules
不修改 prompt after seeing v5 validation result

唯一允许变化的，是实验明确指定的自变量。

例如 Exp8：

只允许 evidence condition 改变

Exp9：

只允许 method/baseline architecture 改变

Exp10：

只允许 candidate information visibility 改变
二、所有实验统一输出格式

非常建议你以后所有 CSV 都统一这些字段。

experiment_id
condition
method
event_id
run_id
semantic_type
source_id
source_family

benchmark_sha256
method_sha256
prompt_sha256

oracle_candidate_id
selected_candidate_id

decision
oracle_correct
closure_pass
strict_success
wrong_select
abstain

failure_stage
failure_reason

m13_status
m14_status
m15_status
ranking_status
m16_status
closure_status

api_error
timeout

llm_calls
input_tokens
output_tokens
total_tokens
latency_ms

random_seed
timestamp

其中：

decision ∈ {SELECT, ABSTAIN}

一定要区分：

wrong_select

和：

abstain

因为你的论文一个核心价值就是：

错误证据或不确定情况下，系统更倾向于 fail closed，而不是执行错误 patch。

三、Exp8：Evidence-Dependence Counterfactual Audit
8.1 研究问题

这是最重要的新实验。

要回答：

ECR 的高准确率究竟依赖当前 evidence，还是 LLM 依靠对公开 RFC 的预训练记忆也能做对？

你的 RFC 来源都是公开规范，所以这个质疑非常现实。

8.2 父 benchmark

使用已经完成 full-online 测试的：

benchmark/
external-real-holdout-v5-blind-large-robustness-variants/
candidate-id-permute

原因是它已经将 event-level oracle ID 打散：

CAND_001 = 71
CAND_002 = 88
CAND_003 = 105

这样 Exp8 不会同时受到固定 candidate-ID 的干扰。

8.3 固定项

完全保持：

event_id
semantic_type
subject
predicate
candidate values
candidate operations
candidate identifiers
oracle
M13
M14
M15
ranking
M16
OWL closure

唯一修改 evidence。

8.4 必跑 5 个条件

我建议不是之前的 4 个，而是增加一个更严格的 NO_EVIDENCE_MASKED_SOURCE。

Condition A：NORMAL

原始正确 evidence。

E_i → Evidence_i

作为 control。

Condition B：NO_EVIDENCE

删除 evidence 正文，但保留其他 metadata。

例如：

"evidence": ""

或者统一：

"evidence": "[EVIDENCE REMOVED]"

不要每条写不同提示。

Condition C：NO_EVIDENCE_MASKED_SOURCE

比 B 更严格。

同时删除：

evidence text
RFC number
document title
section identifier
source-specific clue

但仍然保留：

subject
predicate
semantic_type
candidate set

原因是：

如果只删正文、仍告诉模型：

RFC 4035

模型仍可能靠预训练记忆 RFC。

这个条件可以真正测试：

没有 evidence，也没有明确 document identity 时，还能不能做对？

Condition D：SHUFFLED_EVIDENCE

给事件 \(E_i\) 换成别的事件 \(E_j\) 的 evidence。

约束：

$$ i\neq j $$

并且：

$$ semanticType(E_i)=semanticType(E_j) $$

最好进一步要求：

$$ source(E_i)\neq source(E_j) $$

例如：

TEMP → 只能换 TEMP
GRE  → 只能换 GRE
Cross-Sentence → 只能换 Cross-Sentence

不要把 TEMP evidence 随便换给 GRE，否则任务会过于简单。

推荐固定：

seed = 20260903

形成一个 permutation map：

evidence-shuffle-map.csv

内容：

event_id
original_source
replacement_event_id
replacement_source
semantic_type
seed

生成一次后 freeze。

Condition E：IRRELEVANT_SAME_SOURCE

这个条件非常有价值。

对事件 \(E_i\)，仍然使用同一个 RFC，但是换成与 target subject/predicate 不相关的 section。

例如：

正确:
RFC 4035 §4.3

替换:
RFC 4035 §2.1

要求：

same source
different section
not containing target support statement

最好规定：

与原 evidence 至少相隔 2 个 paragraph / section

并人工/程序确认 replacement text 不包含：

oracle display value
target assertion
关键 replacement relation

这样可以测试：

模型是否只是看到“这是 RFC 4035”就知道答案。

四、Exp8 样本与运行次数

主实验：

$$ 264\times5\times5=6600 $$

次完整 pipeline run。

即：

Condition	Events	Runs/event	Attempts
NORMAL	264	5	1320
NO_EVIDENCE	264	5	1320
NO_EVIDENCE_MASKED	264	5	1320
SHUFFLED	264	5	1320
IRRELEVANT_SAME_SOURCE	264	5	1320

如果 API 成本较高，可以：

先每个条件 264 × 1 pilot

只检查程序。

Pilot 结果不能用来调 M13–M16。

确认无 bug 后再正式 ×5。

五、Exp8 统计

每个 condition 计算：

Oracle accuracy
End-to-end success
Coverage
Abstention
Wrong-select rate
IR incomplete rate
Ranking abstain
M16 abstain

再计算：

$$ \Delta_c = Acc_{\mathrm{NORMAL}} - Acc_c. $$

比如：

NORMAL - NO_EVIDENCE
NORMAL - SHUFFLED
NORMAL - IRRELEVANT

统计方法：

event-clustered paired bootstrap，10,000 replicates。

因为同一个 event 在不同条件下是配对的。

六、Exp8 预注册假设

运行前写：

H8-1:
Correct evidence should produce higher repair success
than no-evidence and counterfactual-evidence settings.

H8-2:
Evidence removal or mismatch should increase abstention
rather than wrong selection if the fail-closed mechanism works as intended.

H8-3:
Masking source identity should further reduce any performance
that can be attributed to memorized RFC-specific knowledge.

注意：

不要写：必须跌到多少才算成功。

避免：

跑完后再人为决定 70% 算显著，80% 不算。

七、Exp8 输出目录
08-evidence-dependence/
    protocol.md
    freeze.json
    shuffle-map.csv

    normal/
    no-evidence/
    no-evidence-masked-source/
    shuffled-evidence/
    irrelevant-same-source/

    evidence-dependence-details.csv
    evidence-dependence-summary.csv
    evidence-dependence-bootstrap.csv
    evidence-dependence-report.md
八、Exp9：Strong Baseline and Compute-Fairness Evaluation

当前简单 baseline 和 ECR 并不是 compute matched，这一点你论文已经主动披露。

Exp9 的目的就是解决：

是不是因为 ECR 调 LLM 更多次，所以才比 direct LLM 高？

九、Exp9 父 benchmark

仍使用：

candidate-id-permute

因为你已经有对应 ECR full-online 结果：

1315/1320
99.62%
262/264 strict

所以不需要重新跑 ECR control。

十、Baseline B1：Direct All-Candidate LLM

给模型一次看到：

event metadata
correct evidence
candidate A
candidate B
candidate C
candidate operations

让模型直接返回：

{
  "decision": "SELECT",
  "candidate_id": "CAND_XXX"
}

或者：

{
  "decision": "ABSTAIN"
}

不允许生成第四个 candidate。

B1 后面必须走相同 closure

不能只算：

candidate accuracy

而是：

Direct LLM
→ SELECT candidate
→ identical OWL patch
→ identical reasoner
→ identical graph delta
→ identical repair/CQ regression

这样比较：

End-to-End Repair Success

才公平。

十一、Baseline B2：M16-Only

这是最重要 baseline。

直接把现在的 fallback verifier 当完整系统：

Evidence
  ↓
CAND_001 → M16
CAND_002 → M16
CAND_003 → M16
  ↓
exactly one supported with high/medium?
  ├─ yes → SELECT
  └─ no  → ABSTAIN
  ↓
same formal closure

完全不使用：

M13
M14
M15
ranking

M16 必须使用当前 frozen M16 prompt。

不能重新设计更弱 prompt。

十二、Baseline B3：Compute-Matched Deliberative LLM

这是审稿人最可能认可的强 baseline。

先从现有 ECR logs 统计：

mean model calls/event
median model calls/event
P95 calls/event

mean input tokens/event
mean output tokens/event
mean total tokens/event

把它写入：

ecr-compute-budget.json

然后冻结 baseline budget。

B3 推荐流程
Call 1：Evidence analysis

模型只分析 evidence：

{
  "facts": [],
  "uncertainties": []
}
Call 2：Candidate comparison

输入：

analysis
+
all candidates

让它比较 candidate support。

Call 3：Final decision

输出：

SELECT candidate
or
ABSTAIN

如果 ECR 平均调用量明显 >3，可以允许：

one self-check call

但必须保证 baseline token/call budget 不超过提前冻结的 ECR budget。

十三、Exp9 Prompt Freeze

非常重要。

Baseline prompt 不能看 v5 baseline 输出再改。

建议：

v1/v3/v4
↓
设计 baseline prompt
↓
freeze
↓
SHA256
↓
run permuted v5

保存：

baseline-prompts/
    direct-all-candidate.txt
    m16-only.txt
    compute-matched-stage1.txt
    compute-matched-stage2.txt
    compute-matched-stage3.txt
十四、Exp9 运行次数

三种 baseline：

$$ 264\times5 $$

每个 baseline 都 1320 attempts。

总计：

$$ 3960 $$

主 backend：

DeepSeek deepseek-chat

不需要再对每个 baseline 重复 Qwen。

十五、Exp9 输出指标

除了准确率还必须有 compute。

End-to-end repair success
Oracle selection
Strict event success

Coverage
Abstention
Wrong selection

Mean LLM calls/event
Median calls/event
P95 calls/event

Mean input tokens/event
Mean output tokens/event
Mean total tokens/event

Median latency
P95 latency

建议最后表：

Method	Repair success	Strict	Wrong	Abstain	Calls/event	Tokens/event
Direct						
M16-only						
Compute-matched						
ECR						
十六、Exp9 统计

所有 baseline 与 ECR：

paired event-clustered bootstrap，10,000 次。

比较：

ECR - Direct
ECR - M16-only
ECR - Compute-matched

同时报告 95% CI。

十七、Exp10：Candidate-Only Shortcut Audit

已有 permutation 只排除了：

literal CAND_002 shortcut。

但还没有排除：

正确 candidate 的字符串本身是不是特别容易辨认。

十八、Exp10-A：ID-only baseline

在原 v5：

always CAND_002 = 100%

这个已经已知。

在 permuted benchmark：

always CAND_002 = 33.33%

不用重新跑模型。

十九、Exp10-B：Candidate-Only LLM

LLM 只能看到：

candidate values
candidate operations

看不到：

evidence
RFC
subject evidence context
Semantic IR

Prompt：

Select the most plausible repair candidate using only the candidate representations. If the available candidate information is insufficient, abstain.

跑：

$$ 264\times5=1320 $$

使用 permuted benchmark。

二十、Exp10-C：Candidate-Only Statistical Classifier

训练数据：

v1/v3/v4

禁止用 v5 做训练。

提取 candidate-level features：

character_length
token_count
digit_count
date_pattern_count
RFC_identifier_count

contains_current
contains_obsolete
contains_supersede
contains_update

operation_type
add_count
delete_count
number_of_triples

candidate_position
candidate_id

做两组。

Model C1：With ID

包含：

candidate_id
position
Model C2：Without ID

删掉：

candidate_id
position

只看 candidate content。

模型用：

Logistic Regression

足够。

如果愿意可以附加 Random Forest，但 Logistic Regression 是主 probe。

二十一、Exp10 测试集

统一测试：

candidate-id-permute v5

重点看：

C2 without ID

如果它接近 chance：

$$ 33.33\% $$

说明 candidate 文本本身没有明显强 shortcut。

如果很高，就必须分析原因。

二十二、Exp10 不能做的事

不要：

看 v5 哪些 candidate 常见
↓
设计特征
↓
重新测试 v5

这会形成 post-hoc leakage。

特征表运行前 freeze。

二十三、Exp11：Strict Historical Provenance Benchmark

这是解决：

论文到底是真正 semantic drift，还是 current-evidence correction。

你当前 strict provenance audit 是：

$$ N=0 $$

而论文已经明确承认这一边界。

二十四、Exp11 的目标

不要强行追求 264 条。

建议：

最低目标：30 events
推荐目标：60 events
理想目标：90 events

质量比数量重要。

二十五、Exp11 Admission Criteria

一个 event 必须同时满足：

Criterion 1

存在明确旧文档：

$$ D_{t_0} $$
Criterion 2

旧文档明确支持旧 assertion：

$$ \Supp(a_0,D_{t_0}) = supported. $$
Criterion 3

存在明确 successor/current document：

$$ D_{t_1} $$
Criterion 4

存在可验证 lineage：

obsoletes
supersedes
updates
replaces
new version of
Criterion 5

新 document 不再支持旧 assertion：

$$ \Supp(a_0,D_{t_1}) \neq supported. $$

最好能做到：

explicit contradicted

而不是只有没提到。

Criterion 6

新 document 明确支持 replacement：

$$ \Supp(a^*,D_{t_1}) = supported. $$
Criterion 7

t0/t1 evidence 必须有具体 locator：

RFC number
section
paragraph
line/span
二十六、不要强行平衡三个类型

严格历史 provenance 最自然的是：

Temporal-Version

GRE/Cross-Sentence 未必有清晰版本演化。

因此：

如果只找到 40 个 Temporal strict-drift event，也可以。

论文写：

strict historical temporal-drift subset

比人为凑 GRE/CSS 更可信。

二十七、Exp11 Sampling Protocol

先定义 eligible source pool。

例如：

RFC successor relationships

再按照确定规则：

sort by RFC/source ID
↓
scan eligible pairs
↓
include if all criteria satisfied

绝对不能：

先跑 ECR
↓
选做对的 event

构造者不得读取：

M13 output
M14
M15
M16
ranking
prediction
二十八、Strict Gold CSV
strict-provenance-gold.csv

字段：

event_id
semantic_type

old_document
new_document

lineage_relation
lineage_evidence

old_assertion

t0_support_text
t0_location

t1_old_status
t1_old_status_text
t1_old_location

replacement_assertion
t1_replacement_support_text
t1_replacement_location

candidate_1
candidate_2
candidate_3
oracle

reviewer
review_status
二十九、Strict benchmark candidate ID

从一开始就做 per-event permutation。

不要再：

oracle always CAND_002

固定：

seed = 20260906
三十、Exp11 正式运行

Frozen ECR：

DeepSeek
5 runs/event

不调任何参数。

报告：

Attempt success
Strict event success
Wrong select
Abstain
Coverage

如果只有 30 events：

30 × 5 = 150 attempts

完全可以。

三十一、Exp12：Independent Human Annotation

现在你的第二 route 是 heuristic，并不是真正 second annotator。

这个实验用于解决 oracle reliability。

三十二、Exp12 样本

推荐：

$$ 90 $$

即：

30 TEMP
30 GRE
30 Cross-Sentence

从 candidate-ID-permute benchmark 抽。

随机种子：

20260907

生成：

human-audit-sample.csv

一次 freeze。

三十三、Annotator B 可以看到
event metadata
source assertion
evidence
candidate values
candidate operations

不得看到：

private original oracle
Annotator A judgment
model prediction
M13
M14
M15
M16
candidate scores
三十四、B 必须标什么

每条：

semantic_type
evidence_sufficient

oracle_candidate
or
UNDECIDABLE

confidence

supporting_span
reason

建议：

confidence ∈ {high, medium, low}
三十五、Annotation Manual

一定要提前写：

annotation-guidelines.pdf/md

里面解释：

什么叫 supported
什么叫 contradicted
什么叫 underspecified

TEMP 怎么判断
GRE 怎么判断
Cross-Sentence 怎么判断

什么时候可以 UNDECIDABLE
三十六、Exp12 指标

分别报告：

Semantic type
raw agreement
Cohen's kappa
Evidence sufficiency
raw agreement
kappa if variance permits
Oracle
raw agreement
Cohen's kappa

由于 permutation 后 oracle ID 有 variance，所以这次真正可以算。

三十七、Disagreement

所有 disagreement：

A
+
B
↓
adjudication

保存：

annotation-disagreements.csv
annotation-adjudication.csv
三十八、如果发现原 oracle 错了怎么办

这一点非常重要。

绝对不能静默修改 v5。

如果 second human audit 发现 oracle 确实错误：

original v5 remains frozen

新建：

v5-annotation-corrected-audit

然后报告：

Original frozen result
Corrected-oracle sensitivity result

这种处理方式反而比偷偷改 benchmark 更专业。

三十九、Exp13：Semantic IR Fidelity Audit

这是验证：

Semantic IR 到底是不是“语义中间表示”，还是只是为了选 candidate 有用。

当前 Temporal gold 已显示：

effective anchor 大幅提高；
complete frame 只有 38.6%；
relation type accuracy 反而下降。

所以必须补 GRE 和 Cross-Sentence。

四十、Exp13 样本

新增：

GRE = 30
Cross-Sentence = 30

Temporal：

复用已有 88

如果精力允许，GRE/CSS 也做 50。

四十一、GRE Gold Schema
{
  "subject": "",
  "general_rule": "",
  "exception_trigger": "",
  "exception_rule": "",
  "priority_relation": "",
  "result": "",
  "supporting_span": ""
}

Critical slots：

general_rule
exception_trigger
exception_rule/result
priority_relation
四十二、Cross-Sentence Gold
{
  "subject": "",
  "statement": "",
  "qualifier": "",
  "scope_target": "",
  "scope_relation": "",
  "result": "",
  "supporting_span": ""
}

Critical：

qualifier
scope_target
scope_relation
result
四十三、Exp13 比较对象

GRE/CSS：

M13
vs
M13 + M14

TEMP：

M13
vs
M13 + M15

Candidate content 全程隐藏。

四十四、Exp13 Metrics
1. Slot accuracy

每个 critical slot 单独：

correct
incorrect
missing
unsupported
2. Slot Precision/Recall/F1

如果允许多值 slot。

3. Complete-frame accuracy

所有 critical fields 同时正确。

4. Evidence Support Rate
$$ ESR= \frac{ \text{populated IR fields supported by evidence} }{ \text{all populated IR fields} }. $$
5. Hallucinated Field Rate
$$ HFR= \frac{ \text{unsupported populated fields} }{ \text{all populated IR fields} }. $$
6. Critical Missing Rate
$$ CMR= \frac{ \text{missing critical fields} }{ \text{all required critical fields} }. $$
四十五、Exp13 不要用 LLM 当 judge

你的核心 claim 本来就在讨论 LLM。

所以不要：

DeepSeek output
→ GPT judge correctness

这会形成循环验证。

用：

human gold
+ deterministic normalized matching

必要时人工 adjudication。

四十六、Exp14：Statistical Robustness Audit

你的 event-clustered bootstrap 已经是正确方向，因为五次 run 不能当 1320 个独立样本。

现在补 source-level sensitivity。

四十七、Exp14-A：RFC/Source-Cluster Bootstrap

如果每个 event 有：

source_rfc

优先按 RFC document cluster。

否则按：

source_family

bootstrap。

方法
Level 1

随机有放回抽：

RFC/source groups
Level 2

被抽到的 source 中带入其所有 events。

Level 3

每个 event 带全部 5 runs。

做：

10,000 bootstrap replicates

随机种子：

20260909
四十八、Exp14-B：Leave-One-Source-Out

如果 source groups 数量不算太多，非常建议再做：

Leave one RFC/source family out

每次去掉一个 source：

$$ Acc_{-s} $$

报告：

min
max
mean
range

如果去掉任何一个 source 后仍然 99% 左右：

说明结果不是被某一个 RFC family 主导。

四十九、Exp14-C：Robustness Pair CIs

补之前几个实验的 paired CI。

k3 vs k5
event paired bootstrap
k3 vs k10
DeepSeek vs Qwen
Original vs Permutation

最后一个你已经做过。

五十、Exp14-D：Zero-Wrong-Selection Uncertainty

现在：

0 wrong selected

不要只写：

risk = 0.

增加：

95% CI / upper confidence bound

可以按：

event-level wrong-selection count

使用：

Clopper-Pearson

或者 Wilson。

论文措辞：

zero observed wrong selections

而不是：

true semantic risk is zero.

五十一、Exp15：Real End-to-End Audit Trace

这不是准确率实验，而是验证你的：

auditability claim。

当前真实案例主要是失败 chain，而成功案例还是 schematic X→Y。

五十二、成功案例怎么选

为了避免 cherry-picking，运行前规定：

对每个 semantic type，将 event_id 升序排列，选择第一个满足 5/5 全成功且日志完整的事件。

于是：

1 TEMP
1 GRE
1 Cross-Sentence

共三个。

不要手挑“最好看的”。

五十三、每个真实案例必须导出
Stage 1
original assertion
Stage 2
raw evidence
Stage 3
EvidenceFocus output
Stage 4

完整 M13 IR：

{}
Stage 5

如果执行：

M14 output
or
M15 output
Stage 6

每个 candidate 的：

display value
formal operation
Stage 7

每个 candidate 的：

$$ \phi(c_i) $$

具体：

s_tok
s_exact
s_family
s_code
s_num
s_sem
p_neg
Stage 8

必须给：

aggregate score
threshold
margin
gate decision
Stage 9

如果触发 M16：

candidate verdict
confidence
reason
Stage 10

最终：

SELECT
Stage 11

Declared patch：

DELETE triples
ADD triples
Stage 12

Actual：

RDF graph delta
Stage 13
reasoner = consistent/inconsistent
Stage 14
repair/CQ checks
Stage 15
FINAL SUCCESS
五十四、Exp15 再保留两个失败案例

你已经有：

H5_E017

以及 permutation：

H5_E003

因此最终 audit cases：

3 successful
+
2 abstention/failure

非常完整。

五十五、所有实验之外还有一个必须完成的任务：Agg Reproducibility Audit

这不是新实验，但优先级跟 P0 一样高。

当前论文给出了：

$$ \phi= (s_{\rm tok}, s_{\rm exact}, s_{\rm family}, s_{\rm code}, s_{\rm num}, s_{\rm sem}, p_{\rm neg}) $$

但只说：

$$ S=\Agg(\phi,\tau_E). $$

没有把 Agg 真正展开。

你必须从当前 frozen code 原样提取：

weight_tok
weight_exact
weight_family
weight_code
weight_num
semantic_bonus
negation_penalty
direction penalty
type-specific bonus
normalization
clipping

不要重新调。

五十六、Reproducibility Package 最终应包含
artifact/
│
├── benchmark/
│
├── prompts/
│   ├── m13.txt
│   ├── m14.txt
│   └── m16.txt
│
├── schemas/
│   ├── temporal-ir.json
│   ├── gre-ir.json
│   └── cross-sentence-ir.json
│
├── scorer/
│   ├── aggregation.py
│   └── thresholds.json
│
├── experiments/
│   ├── 08-evidence-dependence/
│   ├── 09-strong-baselines/
│   ├── 10-candidate-shortcut/
│   ├── 11-strict-provenance/
│   ├── 12-human-annotation/
│   ├── 13-ir-fidelity/
│   ├── 14-statistical-audit/
│   └── 15-audit-trace/
│
├── environment/
│   ├── requirements.txt
│   ├── python-version.txt
│   ├── reasoner-version.txt
│   ├── rdf-library-version.txt
│   └── model-configs.json
│
└── MANIFEST.sha256
五十七、建议的实验执行顺序

严格按：

STEP 0
Validation protocol freeze
        ↓
STEP 1
Exp8 Evidence dependence
        ↓
STEP 2
Exp9 Strong baseline
        ↓
STEP 3
Exp10 Candidate shortcut
        ↓
STEP 4
Exp11 Strict provenance
        ↓
STEP 5
Exp12 Second human annotation
        ↓
STEP 6
Exp13 Semantic IR audit
        ↓
STEP 7
Exp14 Statistical audit
        ↓
STEP 8
Exp15 Real audit trace
        ↓
STEP 9
Freeze all results
        ↓
Update manuscript
五十八、每做完一个实验，不要立刻改方法

这是最重要的纪律。

比如 Exp8 跑出来：

No evidence = 85%

不要马上：

“那我改 M13 prompt，让它更依赖 evidence。”

这样会破坏现在最有价值的：

frozen-method validation

应该：

记录真实结果
↓
解释
↓
如确实要修改方法
↓
那就是下一版本 ECR-v2

不能再算当前 frozen ECR 的 blind validation。

五十九、建议的总实验量

LLM 调用规模大概是：

实验	Attempts
Exp8	6,600 pipeline runs
Exp9	3,960 baseline runs
Exp10 candidate-only LLM	1,320
Exp11 strict provenance	N × 5
Exp12	人工，不调用
Exp13	主要复用已有输出/人工 gold
Exp14	离线统计
Exp15	日志导出

Exp8 是最大的一块。

如果 DeepSeek API 成本较高，先全部做 1-run pilot，只用于程序 QA，不做论文统计；确认没有代码问题后，再一次性正式 ×5。

六十、最终论文会新增的 RQ

全部完成后，我建议你的 Evaluation 重新形成：

RQ1

Overall effectiveness.

RQ2

Component contribution and Semantic IR fidelity.

RQ3

Formal closure safety boundary.

RQ4

Candidate pool / backend / identifier robustness.

RQ5

Does ECR genuinely depend on normative evidence?

Exp8。

RQ6

Does ECR outperform strong and compute-comparable LLM baselines?

Exp9。

RQ7

Does the benchmark contain exploitable candidate-side shortcuts?

Exp10。

RQ8

Does performance extend to provenance-complete historical drift cases?

Exp11。

Annotation、source-cluster statistics 和 audit case 可以放 validation subsection，不一定都单独一个 RQ。

最关键的四个实验

如果你确实准备全部跑，我建议前四个一定严格按照上述 protocol：

Exp8：Evidence dependence
Exp9：Strong baseline
Exp10：Candidate-only shortcut
Exp11：Strict provenance

这四个做完，基本就是直接对准目前最危险的四个 Q1 审稿问题：

是不是靠模型记忆？
是不是靠更多 LLM 调用？
是不是 benchmark 有 shortcut？
到底是不是真正 semantic drift？

而 Exp12–15 是进一步把 benchmark validity、Semantic IR validity、统计稳健性和 auditability 做实。

你执行时，建议从 Exp8 开始，并且第一步不是跑 API，而是先创建 08-evidence-dependence/protocol.md 和五种 benchmark variant。这样后面整个实验链不会乱。