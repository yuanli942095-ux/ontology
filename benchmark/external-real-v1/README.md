# External Real V1 Benchmark Scaffold

This directory is reserved for externally sourced, real-world validation events.
It must not contain controlled or synthetic events from `semantic-v2`.

Current status:

- `READY` events: 0
- data collection status: pending
- intended use: external validation only, not training or benchmark tuning

Use `protocol.md` before adding any event.
