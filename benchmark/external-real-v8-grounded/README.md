# External Real V8 Grounded

Public events retrieved from official cached full text using event-target
metadata only. Blind files do not contain candidate lists, Oracle labels,
formal-policy allowed values, gold answers, or construction notes.

- READY after support PASS: 151 events across 10 domains (target was 198;
  WARN/FAIL were replaced then dropped instead of kept).
- Structured excerpt rate is 0. Retrieval success is not semantic support.
- Do not copy `private/`, `rules/`, or candidate CSVs into this revision.
- `rules/` is blind-forbidden if added later for offline closure.
